# QMS App - Development Guidelines

## プロジェクト概要
GQP × ISO9001に基づく品質管理システム（QMS）アプリ。
中小〜中堅製造業向けのWebアプリ（PC/スマホ両対応、SaaS化・マルチテナント対応）。

## 開発プロセスルール

**機能がひとつ完成するごとに、以下を必ず実施すること：**

1. **自動テストの全件実行**
   - `npm test` または `npm run test:all`
   
2. **セルフレビュー・リファクタリング**
   - 開発原則が守られているか確認
   - コードの重複、命名、責務分離をチェック
   
3. **潜在的問題のチェック**
   - パフォーマンス問題
   - セキュリティリスク
   - N+1クエリ等のDB問題
   
4. **ユーザーによる動作確認**
   - 機能完成時に `notify_user` で確認依頼
   
5. **ドキュメント更新**
   - TODOリスト
   - 機能一覧（更新時）
   - テーブル定義・ER図（更新時）
   - シーケンス図（更新時）
   - このCLAUDE.md

## ドキュメント位置

| ドキュメント | パス |
|-------------|------|
| **TODOリスト** | `docs/TODO.md` |
| **機能一覧** | `docs/FEATURES.md` |
| **テーブル定義** | `docs/DATABASE.md` |
| **ER図** | `docs/ER_DIAGRAM.md` |
| **シーケンス図** | `docs/SEQUENCE_DIAGRAMS.md` |
| **API仕様** | `docs/API.md` |

## 技術スタック

| レイヤー | 技術 |
|----------|------|
| **フロントエンド** | Next.js 14 (App Router) / React 18 |
| **スタイリング** | Vanilla CSS（レスポンシブ対応） |
| **バックエンド** | Next.js API Routes |
| **データベース** | PostgreSQL（マルチテナント対応） |
| **ORM** | Prisma |
| **認証** | NextAuth.js |
| **テスト** | Jest + React Testing Library |

## ディレクトリ構造

```
qms-app/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── (auth)/             # 認証関連ページ
│   │   ├── (dashboard)/        # ダッシュボード
│   │   ├── api/                # API Routes
│   │   └── layout.tsx
│   ├── components/             # UIコンポーネント
│   │   ├── common/             # 共通コンポーネント
│   │   ├── lots/               # ロット関連
│   │   ├── deviations/         # 逸脱関連
│   │   └── ...
│   ├── lib/                    # ユーティリティ
│   │   ├── db/                 # DB関連
│   │   ├── auth/               # 認証関連
│   │   └── utils/              # 汎用ユーティリティ
│   └── types/                  # TypeScript型定義
├── prisma/
│   └── schema.prisma           # DBスキーマ
├── docs/                       # ドキュメント
├── tests/                      # テスト
└── public/                     # 静的ファイル
```

## 設計原則

### 1. 監査証跡（Audit Trail）
- 全レコードの変更履歴を保持
- 削除不可、取消（無効化）で対応
- 変更者・日時・理由を記録

### 2. クローズ条件
- チェックリスト全OKでないとクローズ不可
- 逸脱・苦情は「有効性確認」必須

### 3. リンク強制
- イベント作成時に対象ロット必須
- 空のまま進めない設計

### 4. 入力負荷軽減
- 選択式（プルダウン）中心
- 写真添付で最低限成立
- 次にやることを自動表示

## コミット規約

```
feat: 新機能追加
fix: バグ修正
docs: ドキュメント更新
style: コードフォーマット
refactor: リファクタリング
test: テスト追加・修正
chore: ビルド・設定変更
```

## MVPフェーズ

| Phase | 内容 | ステータス |
|-------|------|:----------:|
| MVP1 | ロット台帳 + 出荷判定 + 逸脱 + 監査証跡 | 🔄 計画中 |
| MVP2 | 変更管理 + 苦情/品質情報 | ⏳ 待機 |
| MVP3 | 供給者/委託先 + 教育/力量 | ⏳ 待機 |
