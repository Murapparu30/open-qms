# QMS App 開発記録

## 2025-12-29 セッションサマリー

### 今日やったこと ✅

---

#### 1. CAPA作成フォームのバグ修正 🐛

**問題**: 「担当者が見つかりません」エラー

**原因**: フォームが `name` を `id` として使用

**修正**: `/api/users/assignees` 新規作成 → 正しいUUID取得

**結果**: ✅ CAPA作成成功

---

#### 2. 文書管理モジュール追加 📄

**新機能**:
- SOP・手順書の作成・編集
- 自動版管理（改訂履歴追跡）
- 承認フロー（ドラフト→レビュー→承認→発行）
- カテゴリ・有効期限管理

**作成ファイル**:
- Prisma: `Document`, `DocumentVersion` モデル
- API: 6エンドポイント
- UI: 4ページ + サイドバー

**結果**: ✅ `DOC-2025-001` テスト作成成功

---

### 検証結果

| 機能 | 結果 |
|------|------|
| CAPA作成 | ✅ バグ修正完了 |
| 文書一覧 | ✅ フィルタ動作 |
| 文書作成 | ✅ 版管理動作 |
| 承認フロー | ✅ UI実装済み |
| ビルド | ✅ 47ルート成功 |

---

### 次回やるべきこと 📋

#### 優先度: 高
1. **承認フローの動作テスト**
   - レビュー申請 → 承認 → 発行の一連フロー確認

#### 優先度: 中
2. **メール通知の本番設定**
3. **本番デプロイ実行** (Railway + Vercel)

#### 優先度: 低
4. **追加機能** (CSVエクスポート、ダッシュボードグラフ)

---

### 新規ファイル（本日追加）

```
src/app/api/users/assignees/route.ts          ← CAPA修正用
src/app/api/documents/route.ts                 ← 文書管理
src/app/api/documents/[id]/route.ts
src/app/api/documents/[id]/submit-for-review/route.ts
src/app/api/documents/[id]/approve/route.ts
src/app/api/documents/[id]/reject/route.ts
src/app/api/documents/[id]/versions/route.ts
src/app/(dashboard)/documents/page.tsx
src/app/(dashboard)/documents/new/page.tsx
src/app/(dashboard)/documents/[id]/page.tsx
src/app/(dashboard)/documents/[id]/edit/page.tsx
```

---

### 技術メモ

- **開発サーバー**: `npm run dev`
- **DB起動**: `docker-compose up -d`
- **テストユーザー**: `admin@example.com` / `admin123`

---

最終更新: 2025-12-29
