# QMS App 開発記録

## 2025-12-25 セッションサマリー

### 今日やったこと ✅

---

#### 1. 添付ファイルAPI 500エラー修正
- **原因**: PostgreSQLデータベースが起動していなかった
- **対応**:
  - Docker Desktop起動
  - `docker-compose up -d` でPostgreSQLコンテナ起動
  - `npx prisma db push` でスキーマ同期
  - `npx prisma db seed` でテストデータ作成
- **結果**: `/api/attachments` 正常動作確認
  - GET → 200 OK
  - POST → 201 Created（ファイルアップロード成功）

---

#### 2. 苦情/CAPA詳細ページに添付機能追加
- `src/app/(dashboard)/complaints/[id]/AttachmentWrapper.tsx` 作成
- `src/app/(dashboard)/capas/[id]/AttachmentWrapper.tsx` 作成
- 両詳細ページに `<AttachmentWrapper>` コンポーネント統合
- entityType: `COMPLAINT`, `CAPA` で連携

---

### 検証結果

| 機能 | 結果 |
|------|------|
| 逸脱詳細 添付ファイル | ✅ アップロード・表示動作確認 |
| 苦情詳細 添付ファイル | ✅ セクション表示確認 |
| CAPA詳細 添付ファイル | ⚠️ コード統合済み（CAPA作成に別バグあり） |

---

### 発見した問題点 🐛

1. **CAPA作成フォーム**: 「担当者が見つかりません」エラー
   - assigneeIdのドロップダウン値がバックエンドで認識されていない可能性

---

### 次回やるべきこと 📋

#### 優先度: 高
1. **CAPA作成フォームのバグ修正**
   - assigneeIdの値がUserテーブルと一致しているか確認
   - `/api/capas` POSTハンドラのバリデーション確認

#### 優先度: 中
2. **メール通知の本番設定**
   - SMTP設定（SendGrid/AWS SES等）
   - 実メール送信テスト

3. **本番デプロイ実行**
   - Railway PostgreSQL作成
   - Vercel連携
   - 環境変数設定
   - 初期ユーザー作成

#### 優先度: 低
4. **追加機能検討**
   - CSVエクスポート
   - パスワードリセット（メール）
   - ダッシュボードグラフ

---

### 技術メモ

- **開発サーバー起動**: `npm run dev`
- **DB起動**: `docker-compose up -d`
- **DBリセット**: `npx prisma db push` + `npx prisma db seed`
- **テストユーザー**:
  - `admin@example.com` / `admin123`
  - `qa@example.com` / `qa123`
  - `qc@example.com` / `qc123`
  - `mfg@example.com` / `mfg123`

---

最終更新: 2025-12-25
